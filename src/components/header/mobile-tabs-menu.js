import React from "react";

const MobileTabsMenu = (props) => {

    return (
        <>
            mobile tabs menu is here
        </>
    )
}

export default React.memo(MobileTabsMenu);