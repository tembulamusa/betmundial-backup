import React, { useContext } from "react";
import { Context } from "../../../context/store";

const SureCoinChoices = (props) => {
    const [state, dispatch] = useContext(Context);
    
    return (
        <>
            {}
        </>
    )
}

export default React.memo(SureCoinChoices);